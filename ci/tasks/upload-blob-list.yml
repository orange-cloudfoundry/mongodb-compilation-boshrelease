---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: garland/aws-cli-docker
    tag: latest

inputs:
  - name: dl-versions
  - name: to-upload
  - name: mongodb-compilation-bosh-release

outputs:
  - name: uploaded  

run:
  path: mongodb-compilation-bosh-release/ci/bin/upload-blob-list.sh

